export type User = {
  userId: string
  nom: string
  email: string
  estActif?: boolean
  avatar?: string
  role?: string
}
 

  // public required string PasswordHash { get; set; }

  // public RoleUser Role { get; set; } = RoleUser.Receptionniste;

